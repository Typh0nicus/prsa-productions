## Prša Productions — current project direction

This brief supersedes the retired personal-portfolio direction. `archive/DESIGN_FOUNDATION.retired.md` remains retired and must not be used.

### Brand model

- The primary brand is **Prša Productions**. Preserve the spelling **Prša**.
- Gregor Prša is the lead figure and public face, not the whole company.
- The site must be able to accommodate more collaborators and team members without a structural rewrite.
- Keep attribution honest: do not silently turn Gregor's historic personal credits into company-wide claims.
- Do not invent metrics, testimonials, client results, awards, job titles, or services.

### CURRENT DIRECTION (July 2026, from Gregor): white + blue, Overluce base

- The site is now **light**: #F5F5F5 field, #313336 ink, and **#2F6BFF** as the ONLY accent. The original Overluce-sampled #1033FF was revised July 18 on Gregor's feedback ("too dark blueish... slightly towards the baby blue shade, not exactly that") — the accent must stay in this softened-electric register: never back to indigo-dark #1033FF, never pastel baby blue. The rgba twin is rgba(47,107,255,x); the `.btn--blue` hover darken is #1E53E8. The current dark cinematic hero remains the accepted cover and ends in a crisp cut into the white world, but Gregor explicitly clarified July 17 that the hero is open to deliberate improvement; "front page is fine" was approval, not a permanent freeze. **Exception to the light rule (July 20): the About section is the site's ONE sanctioned dark interlude** (see About section below) — the page's light rhythm is dark cover → white → dark About → white → blue close; do not add further dark rooms without an explicit ask.
- **INTENTIONAL-CHOICE PROTECTION (Typhon, July 27, after the full-page peak review): "we're trying to improve the graphic quality itself, not change intentional things."** Rulings from that review, now law: the LIGHT page scrollbar is intentional ("looks professional like Overluce") - never dark-theme it; the MENU is intentional in order (Work/About/Credits/Contact) and in its dim resting state; the centered Credits headline, the footer band's geometry and diagonal streaks, the hero CTA's sculpted material + static shine, and the COPY EMAIL "+" affordance all stay. Review passes may propose against these only with explicit new evidence, never silently normalize them.
- **Nav theming is BAND-AWARE everywhere (July 27):** the nav midline (scrollY+44) decides - `inHeroBand` (midline above the hero's bottom), `inAboutBand`, and `inTlBand` (midline inside the credits `.tl` instrument panel, measured live via getBoundingClientRect with a lazily re-resolved node since renderCredits rebuilds the panel) all suppress `nav-light` - the wordmark turns white over every dark surface it floats across, including the credits board (Typhon's ask July 27). The old `heroH - 90` early flip painted the dark wordmark onto the dark hero tail for a ~46px scroll window; never flip on section thresholds, always on the midline.
- **STATEMENT-STAT COMBO (Typhon's July 30 revision):** `1.6B+` (Work) stays plain BLUE TEXT; `41 clients` (Credits, via the atomic `.hl-ink` span) is BLACK/INK; the white-on-blue chip belongs to `1.1B+` alone. Lesson kept: any multi-word emphasis inside a split statement must be an ATOMIC span (`.hl`/`.hl-ink` path), never `<strong>` - splitWords slices strongs per word and renders seamed fragments.
- Rail hover is TITLE-ONLY (the caps channel line was tried July 27 and REVERSED by Typhon the same day - do not re-add attribution lines to the rail scrim). Credits->About seam breathes at 160px (`.credits` padding-bottom).
- **WORK IS WATCH-FIRST (Gregor, July 30 afternoon - latest ruling):** the featured rails are immediate proof: see the thumbnail, click, watch the edit. Gregor judged the deep case-study detour low-ROI ("They see video, click video, watch video") and accepted Typhon's replacement: a polished in-site playback dialog. A plain primary click over HTTP(S) opens the `youtube-nocookie` player in place; file://, modified clicks, and unsupported-dialog browsers keep the honest direct YouTube link. This supersedes every older rule that made the Work rail route to a case page. Existing case pages remain in the repo and Credits may still reach them, but do not expand them as the primary Work journey.
- **SHORTS COVERS ARE EDITORIAL ASSETS (Gregor, July 30):** use the exact cover visible on the creator's channel, stored locally under `assets/work-thumbnails/`; never synthesize a blanket `/oar2.jpg` source from `format:"portrait"` (five of the six live Shorts used a different channel-grid frame). `work-videos.json.thumb` is authoritative for both orientations, with the exact remote channel source as `thumbFallback`. Hosted and file:// Work rails must mirror the same 16 items through the pipeline-written `FALLBACK_RAIL_WORKS`.
- Overluce grammar is adopted sitewide: `+ (Label)` kickers open sections (this supersedes the old kicker ban), medium-weight statements, whitespace rhythm, and filled-rectangle `.btn` controls. Do not mechanically add arrows, fake indices, `/ 0N` labels, or agency-style microcopy to make a block seem designed.
- Theme switching: `#nav` gains `nav-light` past the hero (wordmark inverts via `filter:invert(1)`), while the dark clipped menu control stays visually invariant across both fields. Case pages are light via `cs-body`.
- The interaction systems that remain are word-rise, scramble, beam, rails, restrained credit-wall reveals, and loader. Dark-only devices (starfield, smoke plates, aurora), the old Credits constellation, and the bottom HUD are retired but preserved in git history / assets.
- The cinematic loader plays its full 620ms-minimum hold on EVERY load (the once-per-session `prsa-intro-seen` gate was REMOVED July 27 on Typhon's order - he read the skip as breakage); reduced motion still skips the hold. **ENTRANCES ANSWER TO THE LOADER:** `revealLanding()` dispatches `prsa:reveal`, and every section-entrance observer (setupWorkReveal, the credits intro IO) registers through `onReveal()` - nothing may reveal or play beneath the preloader, so a mid-page reload can never land inside a half-finished entrance. Any new entrance animation MUST go through `onReveal()`.
- The entrance cannot trap the page: `<noscript>` hides the loader and removes the initial scroll lock, while a small independent 5.2s watchdog forces `body.in`, removes `body.lock`, and hides a stalled loader if the main inline script ever fails before reveal. `initChrome()` clears that watchdog as soon as the normal reveal succeeds.
- Repo: github.com/Typh0nicus/prsa-productions (main). The complete dark build is the baseline commit — never force-push over it.

## Art direction (dark-era notes below; superseded where they conflict with the pivot)

- Professional, premium, dark production-studio atmosphere.
- Use dimensional backgrounds, restrained spatial grids, layered surfaces, and cinematic image treatment inspired by the supplied Workflow Accelerator/COSMOQ and studio references.
- Primary palette remains near-black with monitor azure and one restrained warm counterpoint. Avoid violet, cream themes, uncontrolled neon, and decorative glow for its own sake.
- The Prša Productions wordmark is the author. Client work and recognizable client identities are the principal proof.
- Mine reference logic, not fingerprints. Do not reproduce Overluce's flags, kicker syntax, menu shape, or exact layouts.
- Avoid generic SaaS dashboards and generic AI-agency polish. The site must still feel rooted in editing, motion, and entertainment.

### Hero

- The current hero is the creator-ensemble composite (`assets/hero/creator-ensemble-merged-v1.png`) with the headline "We edit what you watch.", the deck "Post-production behind some of the world's biggest channels.", and the sculpted "See the work" CTA. It is an approved baseline, not immutable; changes still need to preserve immediate editing clarity and honest creator proof. The CTA is deliberately arrowless after the July 17 anti-arrow pass; the orbiting border light supplies its interaction cue.
- **HERO TEXT ENTRANCE = THE COSMOQ GRAMMAR (July 27, mined from the saved COSMOQ page at implementation fidelity):** two stacked layers - (a) the whole `.hero-title` container rises `translateY(80px) scale(.9) opacity(.001)` over 2s at delay .2s on `cubic-bezier(.16,1,.3,1)` (already the site's `--ease-silk` - COSMOQ's exact headline curve); (b) every CHARACTER is its own span (`splitChars`: words wrapped in nowrap `.chw`, chars `.ch` with `--ci`) dissolving from `opacity 0 / blur(8px) / y14px`, 22ms stagger. The deck runs per-char blur ONLY (no container motion, 8ms stagger, .5s start) and the CTA fades late (1.05s) - COSMOQ's title-leads / furniture-trails order. **NO MASKS anywhere in the hero text - the old word-rise `overflow:hidden` masks clipped the "y" descender; blur dissolves cannot clip by construction. Statements elsewhere keep word-rise.** Gradient trap: per-char spans inside the metallic `<strong>` need `background:inherit` on BOTH `.chw` and `.ch` (+ text clip) - inherit chains through the word wrapper; without it the chars render transparent-on-nothing (invisible).
- **METALLIC DESCENDER PAINT ROOM (July 30):** Inter’s `y` ink falls fractionally below the per-character chrome background box even without an overflow mask. `.hero-title strong .ch` therefore owns `padding-bottom:.12em` plus the equal negative bottom margin. Keep the pair together: it extends gradient paint without changing line geometry.
- **HERO BACKGROUND: A COMMISSIONED ARTIST ARTWORK IS INCOMING (Typhon, July 27).** The AI ensemble will be replaced by art made by a human artist Typhon/Gregor are commissioning - NOT by any thumbnail/frame collage ("The Real Wall" candidate was built and REJECTED; it survives only as `artifacts/hero-wall.html` for reference, never for the live site). Claude's hero job is integration when the art arrives (cut, grade, entrance choreography), not authoring the artwork. The hero CTA stays sentence-case on the sculpted material - "consistency with the button system" is REJECTED reasoning for hero changes; the landing page is deliberately its own world.
- Typographic voice: tight Inter grotesque. The emphasized phrase ("don't see.") carries the metallic silver gradient. Instrument Serif italic was tried and explicitly rejected — do not reintroduce it.
- Interface chrome follows the COSMOQ "sculpted card" recipe (see `Workflow Accelerator/COSMOQ Mining Notes.md`): graduated multi-layer shadows + opposing azure/warm inset rim-lights on dark gradient fills. Flat hairline/square chrome was tried and rejected as a downgrade.
- CTA light system (locked behavior): at rest, the static top-center shine with bloom (the original reference look — do not widen, dim, or remove). On hover/focus, the `.cta-beam` light orbits the border: a conic-gradient highlight masked to the 1px border ring (`mask-composite:exclude`) animating a registered `--beam-angle` property. The light lives INSIDE the hairline — never an overlaid streak element; anything that protrudes past the frame reads broken (offset-path streak version was rejected). On hover-out it completes the current lap back to its start (top center, where the static shine sits) and parks; re-hovering mid-lap continues seamlessly; a second hover-out within the same lap must not queue an extra spin. Implemented via `animation-play-state` + `animationiteration` in `initMotion()` — do not replace with a plain `:hover` animation. The loader's aperture keeps the matching `plSweep` light.
- The `.work` section is `pointer-events:none` and only `.work-rail` re-enables pointers, so the section's decorative surface never steals interaction from surrounding controls.

### Work section (current direction)

- Overluce-inspired dual rails: two opposite-drifting marquee rails of real thumbnails (16 curated works from `work-videos.json`), slowed to 0.35× on hover, with title-only light tabs. Plain clicks open the full-screen in-site Work player while the anchor remains a direct YouTube fallback. The scroll-pinned single-frame stage was rejected; do not resurrect it.
- **WORK PLAYER = LIGHT SCREENING SHEET (July 30, Typhon polish pass):** the modal belongs to the white Work world, not another full-screen dark room. Its field is `#F5F5F5`; the popup wordmark/chrome aligns pixel-for-pixel with the light nav; darkness is contained to ONE rounded, top-lit player module that unifies the viewport and information dock. Blue is reserved for loading/focus/link cues — never radial fog, an all-blue field, client-color ambience, or decorative streaks. Landscape uses a 16:9 viewport over a compact dock; Shorts keep true 9:16 playback and use a deliberate two-column module on wide screens, stacking on narrow screens. The full sheet only fades; the bounded module gets the silk rise/blur. Preserve the native dialog, Escape/backdrop close, focus restoration, Lenis/rail pause, immediate iframe teardown, modified-click/`file://` fallback, and direct YouTube link.
- Work now belongs cleanly to the white field. Do not place figurative continuation art, a black strip scrim, or a dark blur/shadow band behind the rails. Stored backdrop plates remain source material for deliberate future scenes, not default section wallpaper.
- The headline gloss (`.hero-title strong`) is chrome: white top, gray horizon band ~70%, bright bottom lip. Matched to the user's reference; do not flatten.
- Gregor's portrait belongs in the studio/leadership story below the hero.
- Copy states the studio's role plainly; proof is real or absent.

### Case studies (July 27 perfection pass)

- **STATUS (July 30 afternoon): CASE PAGES ARE NO LONGER THE PRIMARY WORK PATH.** The existing pages remain available from their surviving direct/Credits links, but Gregor's watch-first ruling supersedes further homepage case-study investment. If a case page is deliberately maintained, its existing Overluce anatomy remains the rule: client+tags hero -> "+ (Information)" facts table -> "+ (Project Scope)" written paragraph -> deliverables with performance captions -> "(Insights)" line -> "+ (Testemonial)" quote -> ONE "Next Project" path. **The More-work browser is DEAD (Typhon: "next prj being the right one to use"; Overluce agrees - one forward path).** Never re-add a secondary project browser.
- **"+ (Project scope)" slot is LIVE but data-gated:** renders `work.scope` (a Gregor-written honest sentence or two per case) when present in works.json featured, hidden otherwise. Insights/testimonial slots follow the same render-if-present pattern IF real content ever exists - never fabricate scope text, insights, or quotes.
- **THE BELT IS THE LEGACY CASE LIST (Typhon, July 27): featured = a limited curated selection, currently 12, all landscape** - 5 Stokes longforms, Sidemen Guess The Footballer, FutCrunch Kids World Cup, Nellis Guess The WC Footballer, Miniminter Guess The Athlete + America's Most Famous Fast Food, KSI Guess The Language, Ben Azelart Beat The Maze. MrBeast/Unspeakable Shorts left the belt (still in the credits dataset). To change this legacy belt, edit `works.json` featured and run refresh-stats (fills views/likes by id and regenerates the case fallback). The homepage Work rail is independent and is changed only through `work-videos.json`.
- **Before/after comparison sliders: approved device-in-waiting.** Great fit for VFX cases but requires REAL "before" material (raw frames/clips) from Gregor's project files - never synthesize a before. When assets arrive: optional `beforeAfter` field per work + a compare component.
- **Facts strip is HONEST, per-work, and adaptive** (`#csFactsGrid`, grid-auto-flow column so 2-4 facts always fill the row; 2x2 at <=820): Channel (with a 22px inline avatar from `CASE_AVATARS` - needs `display:inline-block`, a global img reset stacks it otherwise) -> Role (omitted when null, never faked) -> Views -> Likes. Stats render through `fmtStat` rounding (62.2M / 304K). The "Platform: YouTube" filler fact is DEAD - never re-add a fact that is identical on every page.
- **Inline player:** clicking the poster plays the edit in place (`youtube-nocookie` embed, autoplay, `.video-frame` at 16/9 or 9/16 for Shorts) while the poster remains a real external link for no-JS/modified clicks. **`file://` keeps the external-link behavior by design - YouTube embeds reject referer-less origins with error 153** (both youtube.com and nocookie; verified), so local file viewing cannot inline-play; verify the player over `python -m http.server`. The in-poster "Watch on YouTube" label is gone (the octagon play is the affordance; the actions row keeps the external link).
- **FALLBACK_WORKS + CASE_AVATARS in case-study.html and FALLBACK_RAIL_WORKS in index.html are PIPELINE-WRITTEN:** `tools/refresh-stats.mjs` regenerates them from `works.json`/`credits.json`/`work-videos.json` on every run. The file:// fallback is production because Typhon opens the site that way; never hand-edit these generated blocks.
- The browser track wears the site's edge-fade mask; case titles keep word-rise (.12em mask padding clears descenders at case scale - verified on "Guess What Age Punched You").
- **Cache-sync rule: styles.css `?v=` must be bumped in BOTH index.html AND case-study.html on every styles change** (case pages sat on remaster-139 while index reached 156).
- **THE SCREENING ROOM (July 30):** the player lives in a full-bleed dark band (`.poster-section`, the hero/About tonal family #0a0d13→#05070a, crisp cuts) lit by the CLIENT's own color — `::before` key light top-left (17% `--case-glow` mix) + `::after` counter-light lower-right (13% mix, hue-rotate(26deg)) — the credits Light Stage grammar at case scale, so every case page is relit by its channel (Stokes red, KSI navy, Miniminter gold). `--case-glow` is set on documentElement in renderCase from **CHANNEL_GLOW**, a hand-curated map in case-study.html mirroring index CHANNEL_PLATES (NOT pipeline-written; unknown channels fall back to #2F6BFF). The shell rides `.cs-stage` (min(1560px, 100% − pads) — wider than `.wrap` on large screens); the actions row sits inside the band in white-alpha with blue-underline hovers. Channel color appears ONLY inside the dark band — the light world keeps the one-blue law.
- **CASE NAV IS BAND-AWARE (July 30):** same midline law as the homepage — onScroll toggles `#nav.over-dark` while scrollY+44 crosses the screening band; the ink wordmark and the ALL WORK link flip white over it. The menu-exit crossfade rule has an `.over-dark` twin (wordmark returns to WHITE through the sheet fade over the band, ink elsewhere).
- **NEXT PROJECT IS A FULL-BLEED SCENE (July 30):** `.nextup`'s blue field spans the viewport — `.next-link` is a block (padding clamp(64px,9vh,110px) 0) holding a `.wrap .next-inner` grid; title clamp(46/5.6vw/86); the boxed in-wrap panel is retired. Focus outline is white inset. The compact utility footer follows directly, mirroring the homepage's blue-close-then-utility ending.
- `hydrateWorks` is protocol-guarded (July 30): on file:// it renders straight from the pipeline-written fallback — no fetch, no CORS console errors.

### Navigation

- Nav controls: 52px dark translucent `#1A1A1A` controls with clipped 9px corners, white bars, a restrained blue inset rim, and the static top light line. They do not turn white on light sections. Spring easing `--ease-spring` on hovers.
- Menu overlay: Overluce's dark menu palette is the base (`#111`/`#1A1A1A`, muted white labels and dividers), while Prša's drifting showreel wall, decrypt interaction, and blue interaction cues remain the innovation layer. Links stay vertically centered in the left column with hairlines running to the screen edge; hover shows a 4px blue dot, whitens the label, nudges the label 14px right, and DIMS the media wall (`.menu-panel:has(.mlinks a:hover) .menu-media-card img`) — the wall dim is a locked user favorite; never drop it in a reskin. The redundant diagonal arrows and right-side contextual route cards were removed by explicit user feedback July 17.
- The nav FLOATS (July 18, per user: "not a full strip covering everything"): over the light world and on case pages there is NO background, blur, or border — just the inverted wordmark and dark chip riding over content, Overluce-style. Content passing under the wordmark at low contrast is the accepted tradeoff. The only nav background anywhere is the gradient scrim over the DARK hero (`no-repeat` — the transparent border row otherwise repeats the gradient as a 1px line); never reintroduce a slab bar in either world.
- Case pages never receive `body.in`, so the homepage nav entrance rule is scoped `body:not(.in):not(.cs-body)` — without the `:not(.cs-body)` the case nav sits at opacity 0 forever (regression fixed July 18).
- Menu choreography (locked by user preference — backdrop-first content staggering was tried twice and rejected as glitchy): the overlay fades as ONE silk sheet (`.mnav` opacity .5s in / .32s quart out). Links stagger in at .12/.2/.28s, media plane .04s, socials .38s. Section jumps happen *while covered* (`window.scrollTo` `behavior:'instant'` before the exit fade) and the destination then plays a `.arrive` entrance (blur/rise .75s) after the menu closes. `exitMenu()` owns close-button, Escape, and link exits (340ms timer); `openMenu` clears the pending exit timer.
- Never give `.mtop`, `.mclose`, or other menu contents their OWN exit animations separate from the sheet — a chrome fade against a still-opaque backdrop blinks the logo out (user-reported bug). The sheet fades as a whole; the pinned page chrome beneath makes the wordmark crossfade invisible.
- Chrome pinning (locked): `.mtop` uses the same `.wrap nav-in` skeleton as `#nav`, so the wordmark and the burger/close button occupy identical pixels in page and menu — verified pixel-equal. Do not give the menu header its own geometry.
- Dark-menu color handoff: `body.menu-open #nav .wordmark img` must temporarily match the menu's white wordmark even when `#nav` is `nav-light`. BUT the moment the sheet starts exiting (`#mnav.exiting`), the underlying wordmark must return to its destination color (`body.menu-open:has(#mnav.exiting) #nav.nav-light .wordmark img{filter:invert(1)}`, plus the `cs-body` twin for case pages) so the pinned wordmarks crossfade white→dark THROUGH the fade instead of ghosting a white copy over light sections and snapping at the end (bug class reported three times — never reintroduce).
- The right side is the drifting showreel wall fed by `works.json` + `menu-videos.json` (menu-only volume library, deduplicated by video id).

### Credits section

- `#credits` sits between Work and About and is fed by `credits.json`, an ordered renown-first array whose order, names, and subscriber figures stay factual.
- **THE MARQUEE DECK (July 27 v2, current — Typhon rejected the flat GPT/Codex chassis: "no need to hardlock on GPT's way"; the Opus board grammar won and was elevated):** the bar is "truly professional like an Adobe Premiere timeline, aesthetically pleasing and organized, a showcase of the highest degree." **Board:** rounded 11px-radius clip pills, 46px tall in 62px lanes with 9px gaps (never crammed edge-to-edge bricks, never 2px-corner flat plates), VIVID plates - `color-mix(in srgb, channel color 90%, #0d1117 10%)` with a top-lit gradient, inset bevels, and a soft drop shadow so clips float; lanes carry deterministic AIR (`LANE_AIR`: per-lane lead-ins, one mid-lane cut gap, ragged out-points - the board must read as an edit's rhythm, NEVER six justified full rows; offsets are hand-set constants, never random) and the in-lane ORDER is OPTIMIZED, not woven (`bestOrder`, July 27: deterministic exhaustive search per lane via Heap's algorithm scoring same-hue horizontal neighbors, same-hue stacking against the lane above, adjacent giants, and monotonic size runs; MrBeast always opens L1); lane MEMBERSHIP adds a hue-diversity term to the weight-balanced greedy (`laneHuePen` - the palette is amber-heavy and pure weight balance once packed five warm plates into one lane, unfixable by ordering alone). Verified floor: every adjacent pair >=38 degrees of hue apart, zero adjacent giants, zero monotonic size runs, ~75ms one-time search. Born from Gregor's "i would have mixed em up a bit more" - never render lanes in raw renown order; clip weights are `--g = 5 + n^1.45 × 58` (superlinear log-subs curve) with a **×1.5 hero multiplier on MrBeast only**; **lane assignment is BALANCED, not round-robin (July 27):** deterministic greedy - biggest first into the lightest lane, `laneCap = ceil(n/6)`, MrBeast seeds L1 - because flexbox distributes per lane, equal per-lane weight pools are the ONLY way a clip's width means the same thing in every lane (round-robin skewed pools until 5.3M Cash & Maverick rendered wider than 21.8M FutCrunch; Typhon caught it). Residual ±5% cross-lane noise from per-lane gap/air counts and name floors is accepted - floors are honest. A side effect keeps beast dominant: L1 = beast + the smallest clips (~595px at 1440); every clip carries a 24px circular avatar chip + its name at 12px (MrBeast instead carries his white wordmark - any wordmark img inside a clip is `.clip-mark` and MUST have `box-shadow:none` or it inherits the avatar ring as a stray box). The chip ring is a DARK PLATE-TINTED mix (`color-mix(var(--plate) 38%, #0a0d12)`), never white - many channel avatars have baked-in colored circles (Carter Sharer's green) and a white ring stacks into a clashing bullseye. Plates whose 90/10 mix exceeds luma .52 (SV2, TBJZL, Miniminter, CouRage, Landen) flip to dark ink via `.clip--light` - never white text on mustard/lime. **Monitor (264px):** a centered MARQUEE identity - 92px avatar ringed in the channel's own `--mon-glow` color (the ring is the channel-color signature; MrBeast presents his transparent lockup, and `.tl-monitor-identity img.is-lockup` MUST keep `background:transparent` - the avatar placeholder background otherwise draws a box around the PNG, the FOURTH occurrence of the beast-box bug class), name 26px, subs caps, persistent clickable "View edits" with NO edit count anywhere (Typhon removed the "· N" entirely July 27 after two reveal iterations - never re-add a count). IDENTITY HOVER LAW: hovering the avatar/lockup OR the channel name grows the mark (scale 1.045 spring) and lights the hint white with a blue underline sweep (the site's link grammar); the whole identity cluster is ONE hit zone: `.tl-monitor-card` wraps mark/name/subs/hint (padded, contiguous - hovering the gaps or the subs line keeps the highlight lit; the old per-child hover flickered crossing gaps) and a single card click toggles the browser (monImg keeps its own click+keyboard handlers; the card handler skips events targeting it). Lockup identities (`:has(img.is-lockup)`) get gap 12px + 5px under the mark so subs/View-edits sit lower for MrBeast/Sidemen-class wordmarks - floating over the **LIGHT STAGE (July 27 FINAL - supersedes both the footage backdrop and dead black; Gregor: "old bg is better cause some vids got shit bg / like thumbn", Typhon: "find something better than both"):** a pure-CSS authored scene lit by the channel's own color - `.tl-monitor::before` key light top-left (17% `--mon-glow` mix) plus floor vignette, `.tl-monitor::after` counter-light lower-right (14% mix, `hue-rotate(26deg)`). Every cut RELIGHTS the whole suite in the incoming channel's hue via the registered `--mon-glow` transition. No imagery on the monitor, ever: thumbnails made quality a lottery, black was a void; the authored stage is uniform-premium across all 41 channels and cuts are instant (zero fetch/decode). NEVER reintroduce footage/thumbnail backdrops or a monitor media link. **HOVER HIERARCHY (July 27, after Typhon's "all the hovers combined feel overstimulating"): the monitor gives ONE calm zone response and micro-reactions live only on true click targets.** The monitor's ONLY hover surfaces are the identity cluster (avatar/lockup/name grow the mark, hint lights with the blue sweep) - nothing else on the monitor reacts; clip lifts are 1px. When adding any new hoverable here, subtract before adding. Browse cards use the exact curated local covers from `work-videos.json` for known Shorts and preserve a consistent horizontal card crop; never show YouTube's pillarboxed default composite. **The cut is a DISSOLVE, not a black dip:** the stage never blanks - the identity fades through (`.tl-monitor-stage.cut .tl-monitor-identity`, .12s out / .32s back, swap at 120ms) while the stage light morphs underneath. Never reintroduce a full-stage opacity dip. TC is a modern glass slug top-right (blurred rgba(8,11,16,.48) pill, 1px hairline, 4px radius) with a SPLIT readout: HH:MM:SS bright (`[data-tc-main]`) and the :FF frames group dimmed to .34 (`[data-tc-frames]`) - paint() writes the two spans, never a single text blob. "PGM" is the broadcast term for the program output bus - the tag means "Prša's program feed"; keep the jargon (it is the authenticity layer) unless Typhon asks to spell out PROGRAM. **The 00:60 ruler tick anchors at `left:calc(100% - 1px)` - at left:100% it pokes 1px past `.tl-scroll` and summons a desktop scrollbar** (found live July 27); `.tl-body` carries `overscroll-behavior-x:contain` so canvas panning never bounces the page. **Engine (all locked):** intro tours the five giants then COMES HOME - cuts back to MrBeast and parks the playhead on his MEASURED out-edge (reduced-motion parks at his clip center); `.clip.is-active` = subtle brightness/top-highlight only (no ring); **pin = SOURCE-LOCK (July 27 v3, supersedes the July 21 frozen-line spec after Typhon asked for Adobe-grade transport):** clicking pins the channel on the monitor while the TRANSPORT STAYS ALIVE - the playhead keeps following the cursor (magnet included), only hover-cuts are gated; clip shows brighten+lift with `aria-pressed` synced, **Escape or re-click releases, clicking another clip transfers**; arrow keys move spatially along the lane (DOM siblings, wrap to adjacent lane) and never steal focus from the browse panel; browse wheel passes through when the row fits, `browseMeasure` clamps its offset on resize, thumb drag handles pointercancel; views render through `fmtViews()` rounding (354M / 26.6M); the boxed SHORT chip is RETIRED ("clanky") - format reads as "· Short" appended to the caps meta line; **switching channels while the browser is open DISSOLVES** (`.tl-browse.retargeting` fades head+track .18s, rebuild fires at 190ms, cards re-stagger via `bCardIn` capped at min(--i,10)×30ms; `closeBrowse` clears the class and timer so an aborted dissolve can never leave the panel invisible); `initTimeline` aborts stale window/document/board listeners via `window.__tlAbort`. **#2F6BFF stays reserved for instrument UI** (playhead, statement box): MrBeast's plate is #2453D6, and no two plates may be byte-identical (Lachlan #3FC441, AveryB #22B4D6). Breakpoints: monitor 236px / 56-42px lanes ≤820, monitor 232px ≤520, identity stays CENTERED at every width, `.tl-body` pans alone on a 1040/980/940px canvas below 1120px (names always whole, touch panning never scrubs). Live roster 41 channels / 1.1B+; dataset 383 items. This bullet supersedes the July 26 "precision editing deck" (flat text-only chassis, left identity, docked pane - rejected July 27) and all older 35-channel/five-track/vivid-unmixed/roster-grid details wherever they conflict.
- **SHORTS IN THE CREDITS BROWSER (July 30; supersedes the older `oar2.jpg` sentence above):** every card keeps the same horizontal 16:9 geometry. Known Shorts are detected through the curated format/ID map, use the exact local cover from `work-videos.json`, and are editorially cropped with `object-fit:cover`; never feed the visible pillarboxed `mqdefault` composite or turn cards into portrait tiles. Actual Short playback remains honest 9:16.
- **MONITOR IDENTITY SWAPS ARE DECODE-ATOMIC (July 30):** a cold monitor-only wordmark must never let the previous channel’s decoded bitmap reappear under the incoming name/glow. Prewarm monitor assets, keep the identity dissolved until the target source is loaded and the live monitor image has decoded, commit image/text/glow/ARIA/browser together, then reveal on the next frame. Every cut carries a revision token so rapid switches cannot complete out of order; a failed custom wordmark falls back to the channel avatar. The light stage itself never blanks.
- The constellation/collage concept is **retired and explicitly rejected** after Gregor saw it and said he was genuinely lost about what it represented. Never restore scattered circles, orbit logic, a central blue slab, hidden hover captions, scale-by-subscriber, bobbing, parallax, or pinned-credit interactions.
- **MENU CONCEPT STATUS (July 21): the channel-switcher preview execution was built and REVERTED same-day** — the user disliked the execution (not the concept). The menu keeps its pre-switcher form (hairline dividers, drifting wall, scramble hover). Do not re-ship destination previews in that form; a future menu concept needs a different execution.
- **THE LIVING EDIT (July 22, current credits execution — answers Gregor's "too uniform / not enticing like a real Adobe timeline" note):** `.tl` is ONE continuous dark panel (monitor + ruler + tracks share the dark material, white-alpha chrome — locked). The design law is CONTROLLED HETEROGENEITY: a real timeline varies on many axes at once, so ours does too, but curated for a showcase (pro AND cinematic, never literal-Adobe chaos). (a) **Clips are FULL VIVID plates** in each channel's authentic brand color (`CHANNEL_PLATES`, 35 entries) — vivid-on-dark reads professional; the old muted L38-46 flattening was a LIGHT-theme fix and must NOT return here. Correct any channel via its one map line. (b) **Beveled**: top-lit `color-mix` gradient + edge bevel = real NLE dimension. (c) **Dramatic length variance** from `max(sub-based width, name-fit floor)` — short-named channels get short clips, long names stay readable, ZERO truncation. (d) **Six layers L1-L6** (Gregor's V→L rename; 6 lanes give room for dramatic lengths + guaranteed fit — no audio/waveform track: the decorative A1 waveform was built then REMOVED as purposeless/ugly, do not re-add). (e) all scrub/cut/pin/pfp-browse interactions and the assemble entrance are preserved. **SIZING IS FLEXBOX-JUSTIFIED (rebuilt July 22, proven 1280-1920px — never revert to absolute --x/--w packing):** lanes are `display:flex`; clips are `flex:var(--g) 1 0; min-width:min-content` with `--g` = subscriber weight (`5 + n*58`). Flexbox natively fills each lane to 100% (no empty space), sizes clips proportional to subs (MrBeast ~5× the smallest — dramatic), floors every clip at its name width (`min-content` = no truncation ever), and cannot overflow. Clips are round-robin-assigned over the renown-sorted credits (spreads giants across the 6 lanes). The playhead magnet snaps are MEASURED from the DOM (`measureSnaps()` in rAF + on resize), not from CSS vars. **The pinned clip is indicated by brighten+lift (NO hard box/ring — a white selection box reads as a glitch); the active clip has no indicator (monitor+glow+playhead show it). Any WORDMARK img inside a clip (`.clip-mark`) MUST have `box-shadow:none` — otherwise it inherits the `.clip img` avatar ring as a stray box around the wordmark (this was the recurring "box on MrBeast" bug).** **THE SIDE/LEFT COLOR-EDGE DEVICE IS PERMANENTLY BANNED ("stop ever doing the side color thing") — never ship `inset 3px 0 0` label edges or any left-edge color strip, anywhere.** This supersedes the graphite-plate and "five brand colors + 8-tone palette" specs below.
- **The Credits treatment is THE TIMELINE (July 21, built under "freedom to blow my mind"; replaces the mosaic entirely):** the credits section is an editor's NLE timeline — the studio's clients presented as clips in a cut, which is the site's whole mythology ("the part you don't see") made literal. Anatomy top-down: dark PGM program monitor (mono "PGM · PRŠA PRODUCTIONS" tag + live timecode, shows the active channel large, MrBeast lockup by default) → timecode ruler (60s span) → five tracks V1–V5 → 35 clip buttons. Clip width = an 8% floor + a log(subscribers) span (18% × scale); pack() normalizes by scaling ONLY the data span — never the floor, which exists so **every channel name renders whole on its clip** (user rule July 21: no truncated names on the board; chips hide under 11% width and `.clip--s` shrinks type under 9.5% so text always wins). The browse track is a VIRTUAL SCROLLER (v15): overflow:hidden with the card row riding `translate3d` under framerate-independent exponential easing (1−exp(−dt·9), wheel ×1.7) — never revert to native `scrollLeft` scrolling; it is integer-quantized and reads as stepped motion. The scrollbar is our own drawn bar (3px rail, proportional thumb, drag with pointer capture, rail-click jumps, blue on hover/drag, hidden when content fits and the row centers). Wheel input MUST NOT scroll the page — the track carries `data-lenis-prevent` and the handler stops propagation; focused cards auto-glide into view. The browse × is a drawn SVG that rotates 90° on hover (the contact-copy '+' grammar) over the blue wipe. The "View edits" hint carries no count. **ANTI-COPY: the main page is `user-select:none` via `body:not(.cs-body)` with `.contact-email` re-enabled as selectable text** — keep the email copyable and keep case pages exempt. Clip colors: the five curated brand colors (MrBeast blue #2F6BFF, Stokes #C22F3F, Ben Azelart #0F93A5, SSSniperWolf #D12C7C, Preston #E06018) + a muted 8-tone NLE label palette for the ensemble (label colors are workflow metadata, honest by definition). Every clip prints its channel name (truncation like a real NLE is fine; aria-label + monitor carry the full name). Interactions: SCRUBBING — the cursor drives the playhead + timecode across the tracks, and hovering any clip CUTS the monitor to that channel (70ms dip, a cut not a crossfade — editors cut); entrance = the edit assembles (clips slide in staggered per lane/position, playhead sweeps 0→30% once). Reduced-motion: static playhead, instant clips, cuts without dip. The ragged right edge of the lanes is intentional (edits end at different times). Do not add idle/ambient motion; the scrub IS the life. Section ~1000px tall.
- **Timeline branding rules (July 21):** channels with white wordmark assets present them on the monitor via `MONITOR_MARKS` (8 currently — extend the map as `assets/wordmarks/*-white.png` files arrive, never fabricate marks). **LawByMike is BANNED from wordmark treatment** — the user ordered `lawbymike-white.png` deleted; he presents via his `lawbymike-fig` pfp only; never recreate or re-add that asset. The name text hides ONLY when the mark literally spells the brand (`MARK_SPELLS_NAME`: MrBeast, Sidemen, KSI, TBJZL); symbol marks (LoverFella's heart, CouRage's lion, What's Inside's (IN), Miniminter's badge) always show mark + name + subs — a logo that doesn't spell the name never stands unlabeled (user rule July 21). **Interaction engine (v3, locked):** the playhead GLIDES (lerp 0.17/frame) and is MAGNETIC — scrub targets within 0.55% snap to the nearest clip in/out edge across all tracks; cuts fire on 70ms hover-intent dwell (fast sweeps must never flicker the monitor); ←/→ arrow keys surf channels with focus/active/monitor/playhead in sync; the field rect is cached (invalidated on scroll/resize). **FIRST PLAYBACK:** on first in-view the edit plays itself once — playhead 0→34% over 4.2s, monitor cutting through the five brand channels in renown order — and ANY pointer contact interrupts it instantly and permanently (it never resumes or loops; this is an entrance performance, not ambient motion). **COLOR-REACTIVE MONITOR:** every cut re-lights the monitor's radial in the active clip's label color via registered `@property --mon-glow` + color-mix at 13% — keep it felt-not-seen; do not raise it into an obvious colored spotlight. Ruler ticks draw in staggered on reveal. Do not replace the glide with instant snapping, remove the dwell timer, or make the performance loop.
- **RETIRED PIN/HOLD + PFP-BROWSE V6/V8 NOTE:** this older frozen-transport/filmstrip direction is superseded by the July 27 source-lock marquee-deck rules above. The abandoned full-screen `#vlib` overlay was removed July 30 when its unused shell became the Work player; never revive it as a second library surface.
- **STATS + THE FULL VIDEO DATASET ARE PIPELINE-DRIVEN (July 21): run `node tools/refresh-stats.mjs`** — it hits YTJobs' public API (`app.ytjobs.co/api/talents/58142?showAll=true` + `/videos?limit=100&page=N`, no key) and rewrites: credits.json subs (name-alias map), the work statement total, FALLBACK_CREDITS, works.json views/likes/`channel` (16/16 attributed), **`data/videos.js`** (all 383 videos with channel attribution as `window.PRSA_VIDEOS`, loaded via a classic script tag before the app script so it works on file:// — never hand-edit, it's regenerated), and data/stats.json. The tool's MANUAL map holds Typhon's hand-confirmed attributions as an authoritative override layer. SSSniperWolf, LawByMike, and Cash have no YTJobs channel — keep their values; never zero or guess. The pfp-browse merges featured works (case links, listed first) with dataset extras (youtu.be links, target _blank). The browse card stagger delay is capped at `min(var(--i),10)` — some channels have 100+ videos. The Apify scraper route stays rejected (secret token cannot ship in static client JS). The monitor readout under the name is the channel's real subscriber count ("509M SUBSCRIBERS") — the user explicitly sanctioned per-channel subs HERE and only here. The MrBeast clip carries `mrbeast-white.png` instead of text (selector must be `.clip img.clip-mark` — plain `.clip-mark` loses specificity to `.clip img`). **No monospace fonts and no corner/safe-area bracket decorations anywhere** — the user reads them as "AI stuff"; all timeline chrome is Inter 600 uppercase letterspaced (the site's utility-caps language). The click-a-clip creator library is DEFERRED until the user supplies a video→channel mapping (works.json lacks channel names; never infer attribution from subscriber counts).
- **The "living field" experiment (subs-weighted avatar sizes, cursor gravity swell, idle breathing, whole-board focus-pull dim) was built July 20 and FULLY REVERTED July 21 on user decision.** The diagnosis that killed it: the grid container "was not made to handle this style" — varied avatar sizes inside identical cells read as inconsistency, and stacked hover systems read as busy, not innovative. More importantly, the user's root reading of Gregor's critique: what the old constellation had was not motion or sizing — it was **a concept different from the norm**. Do not re-add ambient motion layers to the mosaic; the open brief is a genuinely different CREDITS CONCEPT that stays professional (conceptualize first, build after approval).
- **Density is locked (July 20 compaction, v=remaster-102):** the section was 2,147px (37% of the page, "took 3 screenshots to get through" — user) and was compacted to ~1,500px. Roster rows are fixed 152px (reserved-space rule) with ~92px avatars and 12px captions; lead rows minmax(164px,1fr), hero tile ≥328px. The credits head is a CENTERED title card (user-chosen July 20): the statement is text-align:center, margin-inline auto (max 1000px) at the same clamp(34px,3.6vw,56px)/−.038em type as `.work-title` (font consistency holds; position is the deliberate variation), with the kicker absolutely anchored top-left at x=44. Mobile (≤820px) falls back to stacked-left. Do not re-inflate tiles; premiumness here comes from index density, not scale.
- **Interaction signature (July 20): the blue wipe/sweep is the site-wide hover language** — contact-copy (fill wipe), elsewhere links (underline sweep), footer button (fill wipe + label swap), credit tiles (THE CROWNING FLOOD — the full tile floods blue via a ::before scaleX wipe at .42s silk, caption flips white, avatar pops on the plate; hovering any channel gives it the MrBeast blue-plate treatment, which is the section's personalization concept), and foot social boxes (blue fill wipe, icon to white). All sweeps enter left→right and exit right (`--ease-silk`, ~.34–.46s). New hoverables in the light world should speak this language rather than inventing a new one. The MrBeast hero tile keeps its subtle scale-only hover (it is already the crown), and its lockup is centered in the FULL tile via absolute-positioned media with the caption overlaid bottom-left — do not return the caption to document flow (it un-centers the lockup).
- All 35 entries use the purpose-built `assets/wordmarks/*-fig` / `*-row` assets instead of raw avatar crops or generic badge cards. The lead MrBeast cell uses `mrbeast-lockup-white.png` against blue; the data fallback keeps the canonical color lockup path.
- Interaction is subordinate to comprehension: the non-clickable cells stay visually static after their one-time entrance reveal, with no hover wipe or lift that could imply an unavailable action. The wall is semantic list content, not a collection of fake buttons. Reduced motion removes the entrance transition.
- Menu + footer socials include LinkedIn (https://www.linkedin.com/in/gregor-pr%C5%A1a-825ab5265/).
- Lenis is SELF-HOSTED at `assets/vendor/lenis.min.js` (the CDN was unreliable from file:// and killed the scroll weight) — never revert to the CDN.
- **ABOUT AND CONTACT ARE OPEN** as of July 17, 2026 by explicit user instruction. Work them one section per deliberate pass. **About (July 20 rework — the user "really didn't like" the blue-slab version and it was the biggest offender for Gregor's "blue in a lot of spots doesn't make sense" note): a LIGHT editorial split on the white field.** FINAL FORM — **"THE EDIT SUITE" (July 20 sixth pass, user granted full-page creative direction: "consider the rest of the page as a continuous showroom... think, then go"):** About is the site's ONE deliberate dark interlude. Rationale (page rhythm): the scroll was DARK cover → four white rooms → blue strip; the white plateau is why About kept reading empty. Now: dark → white (work, credits) → **DARK about** → white (contact) → blue close. Dark negative space reads as atmosphere, not absence — same reason the hero never felt empty. Construction: `.about` is a flex column, min-height 82svh, near-black gradient (#0a0d13→#05070a, the hero's tonal family), CRISP cuts top and bottom (no gradient seams). `.about-figure` = the portrait ABSOLUTE right at `width:min(62%,960px)`, full-height, cover, **borderless** — a left-edge mask fade (transparent→#000 26%) melts it into the band so the photo's own monitor glow becomes the section's light; slight edge darkening via ::after; scale-in reveal (`.about-figure.in-view`). Copy: kicker top-left (dim white `rgba(242,243,245,.58)`), and the statement "The part you <hl>don't see.</hl>" (white, clamp 44/4.8vw/72) + deck ("An editing studio led by Gregor Prša, cutting for channels the world watches. Collaborators join when a project needs them.", soft white 470px) anchored LOW-LEFT via `.about-scene{margin-top:auto}` — deliberately rhyming with the hero's bottom-left anchor. `data-about-scene` (parallax) sits on the SECTION. The pass-5 fact rows + YTJobs button were deleted as AI filler (user: "whats worse than nothing is ai filler"). **Nav is BAND-AWARE**: `onScroll` computes `inAboutBand` from the nav's midline vs `#about` offsets and suppresses `nav-light` there, so the wordmark returns to white over the dark room (chip is invariant anyway); theme-color follows. Mobile: figure becomes a relative full-width block (order:3, top mask fade) under the copy. No captions, no labels, no meta rows — the scene IS the content.
- **Wordmark halo REVERTED (July 20):** the feathered field-color radial behind the nav wordmark rendered as a giant white egg over every BLUE surface (`nav-light` is active over the footer CTA/credits tile/route cover — "looks broken"). There is NO mask behind the floating wordmark; transient text-through-text during scroll is the accepted cost of the floating nav. Do not retry background-colored halos behind fixed chrome on a page with mixed-color plates. Blue plates on the page are now only the Credits hero tile + footer CTA + next-up panel — blue is punctuation, not paragraphs; do not reintroduce a full-bleed blue About stage. Contact: see the Contact section (email monument). References remain the quality bar and source of compositional logic, not a reason to transplant another studio's assets or exact layouts.
- The stored plates in `assets/backdrops/` (aurora-horizon, smoke-floor, continuation) remain reserved for future passes the user initiates.

### Mined signature systems (July 2026 reference dump — keep, don't regress)

- **Hero word-rise** (Mosaicist/OBSCURA SplitText grammar, vanilla): `splitWords()` wraps each headline word in `.w-mask > .w`; words rise from `translateY(115%)` behind overflow masks on `body.in`, 70ms stagger. `.w-strong` carries the metallic gradient (selector shares the gloss rule).
- **Menu decrypt** (Cinética ScrambleText grammar, vanilla): menu links scramble-resolve left→right over ~340ms on pointerenter (`GLYPHS`, 12 frames). Covert cipher = brand-true. Skipped under reduced motion.
- **Bottom HUD retired July 17, 2026 by explicit user request.** Do not restore the live clock, section/index readout, or cursor coordinates. They were decorative noise and made the Contact/footer ending look unfinished.
- **Credits starfield** (Boiler Lab star canvas) belongs to the preserved dark baseline only. It is retired from the light build and must not be remounted behind the current editorial credit wall.
- Reference saves live in `C:\Users\brnsl\Downloads\` (HAOQI, Boiler Lab, OBSCURA, Cinética, Mosaicist + full asset folders incl. Overluce's complete images). Future material: Cinética image-flash loaders, Boiler Lab moon/typewriter, HAOQI THREE.js field, OBSCURA/Viacheslav layouts — propose before transplanting anywhere and use only in a deliberate section pass.
- Reveal grammar is sitewide (July 17): ALL FOUR statements (Work, Credits, About, Contact) use `.statement-split` masked word-rises gated on any `.in-view` ancestor (`html.js .in-view .statement-split .w`), and all four kickers fade/rise separately. `splitWords` treats non-STRONG element children (the `.hl` highlight boxes — blue box in both About and Contact as of July 20) as ONE atomic rising unit so the box is never sliced into per-word fragments; STRONG still splits per word. Computed Credits copy must be re-split after `innerHTML` updates. The two selector lists in index.html (setupWorkReveal + menu-travel force) must stay in sync.
- The single Credits statement is computed from `credits.length` and factual `subs` values (stored in millions): currently `Editing credits across 41 clients with 1.1B+ subscribers combined.` The atomic `41 clients` phrase stays black/ink; only `1.1B+` uses the white-on-blue chip. Billion totals are conservatively floored to one decimal; sub-billion totals are floored to 10M. Do not restore the retired split counter/right-label layout.
- Credits order is the sole subscriber-based hierarchy. Subscriber figures never render per entry and no longer control asset size or position.
- Keep credit imagery on one static drop shadow and animate transform only. Animated filter chains on large transparent PNGs previously produced visible GPU rectangles under overlays.
- EVERY data set must have an inline fallback in index.html (`FALLBACK_CREDITS` etc.) — the user opens the site as `file://` where fetch() fails; a JSON-only section renders empty for them (reported bug).
- Voice per the archived foundation doc: the section is called "Credits" (never "Clients").

### Work section

- The Work rails are fed exclusively by `work-videos.json`; `FALLBACK_RAIL_WORKS` is an exact pipeline-written inline mirror for file://. The user curates this list directly — do not repopulate it from the case belt or menu library.
- Structure: statement head "Work that's been watched **1.57B** times." followed by two full-bleed drifting rails of large cards. Roles are not displayed anywhere. Card hover (July 20, replacing the white boxed tab the user called gimmicky): a quiet dark gradient scrim rises from the card's bottom edge with the title in white 15px (2-line clamp, max-width 440px), neutral ink shadows and a 3px lift — no boxes, no borders, no blue-tinted depth. Touch/coarse pointers keep titles visible. 110s/128s-reverse drift, 4% edge masks.
- Rail hover SLOWS the drift to 0.35× via `anim.updatePlaybackRate()` (JS in renderWorkWall) — never `animation-play-state:paused`; a dead stop was rejected, 0.2 was too slow. Menu link order: Work, About, Credits, Contact.
- The cursor-following "editing focus light" (bloom + playhead line) was REMOVED July 20 by explicit user feedback ("the hover on videos looks gimmicky n unprofessional"). Do not reintroduce cursor-tracking light effects on the rail cards; hover stays scrim + lift only.
- `setupMotionBudget()` may pause rail tracks only while the entire Work region is offscreen; this is a performance gate, not an interaction state. The menu media track stays paused whenever the menu is closed. Never use this gate to replace the visible 0.35× rail-hover behavior.
- The work statement renders as ONE line (`white-space:nowrap` on desktop, normal under 820px) — the two-line balance was rejected.
- The 1.57B figure is the user-stated real total (July 15, 2026) — Overluce law: numbers must be real or absent.
- Keep the provenance split explicit: `works.json` `career.views` is the dated July 4 YTJobs extraction (1.53B), while `user_confirmed.views` is Gregor's later July 15 total (1.57B) used in the live statement. Neither is the sum of the 16 visible edits (818.41M).
- Work-role data remains in the source for provenance but is intentionally not displayed anywhere, per explicit user feedback July 17.
- The orphaned `work-preview.html` dark prototype was retired July 17. It visibly exposed rejected numbered cards and role labels and had no inbound references; do not restore it as a second Work surface.
- `assets/backdrops/aurora-horizon.webp` (mined from the COSMOQ source assets — their blue→amber horizon light plate) is stored for a future scene (candidate: the Contact resting point). COSMOQ's atmosphere is built from soft pre-rendered glow plates like this; ours is live CSS — adopt plates only for deliberate scene moments.
- Atmosphere is deliberately restrained on the white Work field. Never put figurative imagery, a dark haze plate, or a smoked band between or behind the rails.
- Scroll reveal: `.work-head`/`.work-rail` blur-rise via IntersectionObserver (`setupWorkReveal()`), second rail delayed .14s, disabled under reduced motion.
- The duplicated `.rail-set` clone must stay `aria-hidden` but NEVER `inert` — inert kills pointer events and makes half the drifting cards hover-dead (user-reported bug).
- Menu-nav arrivals animate the target section's CONTENT BLOCKS (`:scope > .wrap, :scope > .work-rails`, staggered 90ms) — never the section element itself; a section-level opacity animation black-holes the viewport (user-reported "insanely broken").
- The old global film-grain overlay is retired from the current light build; the dark baseline preserves it in git history.

### Footer

- The homepage closes with a full blue field (var(--blue)), `+ (Next project)`, the medium-weight statement "Let’s make the next one worth watching.", and a filled ink CTA to `gregor@prsa.productions`. This replaces the rejected thin utility-only ending while staying inside the Overluce palette and type scale.
- All BLUE PLATES share one material recipe (July 18): a soft radial light top-left + one diagonal 118° streak band (Credits hero tile, footer CTA, next-up panel; the About scene left the blue-plate family July 20). The footer kicker/statement word-rise on scroll like every other section (`.footer-cta-inner` is a reveal target).
- The footer CTA uses a restrained vertical label roll over its white horizontal shutter. The label uses contrast-safe difference blending so it never disappears midway across the moving black/white field; reduced motion keeps one static label. The decorative arrow was removed.
- The light signature row remains below it with the Prša Productions wordmark left, copyright centered, and the same YTJobs/Instagram/X/LinkedIn set used by the menu right. There is no back-to-top link. Case-study pages keep the compact utility footer.

### Contact

- Contact hierarchy (July 20 rework, user asked for "creative and professional" — the old layout had two competing giants and read like a settings table): ONE monument. "Talk to Gregor." is a quiet ~28px lead-in beside the kicker (blue `.hl` box on `Talk` kept, small); `gregor@prsa.productions` is the section's dominant element (`.contact-hero`, clamp 54→100px) rising out of an overflow mask on reveal, hovering to full blue with a 3px underline sweep; below the hairline one `.contact-utility` flex row holds the copy-email control (left) and the YTJobs/Instagram/X/LinkedIn text links (right) — the EMAIL/ELSEWHERE labels are gone. This differentiates the two endings: Contact = the email monument, footer = the statement + CTA (user called the blue footer strip "the most professional looking thing you've done to date" — don't restructure it). The copy control's blue wipe rotates the quiet `+` on hover/focus, then changes that aria-hidden symbol to `✓` or `×` while the live region announces the real result; the label and symbol reset together. Do not add explanatory sales copy, fake form fields, service-list prose, or card-grid clutter; no decorative arrows.
- Section and menu jumps land FLUSH (targetY = section top, offset 0; `scroll-margin-top:0`) — the old nav-height offset exposed ~104px of the previous section's tail above the destination once the nav went floating ("its midway through about and credits"). Every section's own top padding provides the breathing room (work 140 / credits 118 / about 110 / contact 118) and keeps its kicker clear of the 86px floating chrome; keep any new section ≥110px top padding.
- Layout must be scroll-stable under lazy loading: the credits roster uses fixed `grid-auto-rows:216px` because content-sized rows grew +156px when lazy avatars decoded, landing every downward anchor jump ~103px short. Any lazy-loaded media needs reserved space (fixed rows/heights or aspect-ratio) so offsets measured at click time stay true.

### Case-study pages

- **Current product status (July 30 afternoon):** retained legacy/direct-link surface, not the homepage Work destination. The Work rails now open the in-site player. Do not add new case-page depth without a fresh explicit decision from Gregor/Typhon.
- Case pages stay light and use the same `+ (Selected work)` editorial grammar. The decorative blue project-index block is retired; a masked title rise and three-column proof row frame each project using public views, public likes, and platform only. Roles are not displayed.
- Section order (current, July 30): hero → facts strip → scope (data-gated) → THE SCREENING ROOM (full-bleed channel-lit player band) → full-bleed Next Project scene → utility footer. The More-work browser is DEAD (see the Case studies section above); the abstract numbered timeline was retired as template UI.
- Route curtain continuity: clicking any project link stores `{title,thumb,format}` in `sessionStorage('prsa-route')`; the case page's head script adds `html.route-arrived` pre-paint so its own `#routeTransition` layer FIRST-PAINTS as the identical blue cover (no white flash), then `departRouteCover()` lifts it upward (`route-depart` → `route-gone`) over the composing page. `project-transition.js`'s `pageshow` reset must yield while `route-arrived` is on (`reset()` guard) — it fires on every load, not just bfcache, and once wiped the arrival cover mid-build. Portrait projects carry `data-project-format` → `route-transition--portrait` (9/16 media frame) and a portrait next-up preview (`next-link--portrait`, oar2 source).
- The source poster is a cinematic interaction stage with a clipped play control, YouTube source label, and fine-pointer depth movement. It remains a direct external link and removes all depth motion under reduced motion.
- The handoff to the following project is a full electric-blue scene backed by that project's real thumbnail. Project titles use a controlled editorial scale (`clamp(52px,6.4vw,94px)`) rather than the retired giant-display treatment. On mobile the three proof cells stay compact, the thumbnail ribbon becomes a 76vw snap strip, portrait posters reduce their inset padding, and the next-project scene stacks vertically.
- All internal project links use the shared `assets/project-transition.js` handoff: a short ink-to-blue curtain carries the Prša Productions label, the selected project's real title, and the clicked thumbnail into navigation. It must preserve native modifier-click/new-tab behavior, reset immediately on `pageshow` for the back-forward cache, fall back cleanly when an image fails, and never intercept under reduced motion.
- Case pages retain the full clipped menu control instead of becoming navigation dead ends. Their overlay uses the homepage's dark silk-sheet chrome and primary/social routes, while the right side is a quiet selected-project panel populated from the canonical title and thumbnail—no fake route number or role. The menu traps focus, locks background regions with `inert`, preserves modifier-clicks, closes through one sheet on Escape/button exits, and routes while covered.

### Mobile direction (August 2, 2026)

- Mobile is a first-class composition, not a scaled desktop fallback. At phone widths the dark hero is slightly shorter than the visual viewport so the white Work world peeks into the first screen; headings use controlled phone sizes, safe-area insets protect all fixed chrome, and the page removes the desktop scrollbar gutter.
- Work rails are user-controlled snap galleries on phones, phone landscape through 1024px, and coarse-pointer tablets: animation stops, the duplicate marquee set is hidden, and each row swipes natively with one full landscape card plus a hint of the next. Desktop keeps the approved opposite-drifting marquee.
- Credits keeps the NLE concept on mobile, but its touch surface is adapted: the monitor and lane labels stay pinned while the board pans, start/end fades reflect the real scroll edge, and clips retain at least a 44px target. The edits browser uses native momentum, 24-card incremental batches, a keyboard-accessible More path, and horizontal 16:9 covers on phones/coarse pointers; crossing back to desktop must eagerly restore the complete custom-scroller row.
- Mobile loading must stay proportional: decode the current monitor identity immediately, prewarm only the eight custom monitor marks when Credits nears view, and decode all other client avatars on demand. The decode-atomic cut and revision token remain mandatory.
- Rotation is a supported state change. Resize, orientation, and visual-viewport changes must recompute band-aware nav color, menu location, timeline geometry, and hero scroll treatment without waiting for another scroll gesture. Phone-landscape restores the About side composition, caps rails/type by height, and keeps the Credits monitor directly beneath the 58px landscape nav. Closing a Work player after rotation restores the originating card as a visible viewport anchor, never a stale absolute scroll coordinate.
- Interactive phone targets are at least 44px even when their visible icon remains smaller. Menu, player, contact, social, Credits, and case-page focus flows must survive touch activation, Escape, orientation changes, and safe-area devices.

### Production readiness

- The homepage publishes factual description, Open Graph/Twitter cards, and `Organization` JSON-LD using only the real brand name, Gregor, contact address, social profiles, wordmark, and approved hero composite. The 1.57B user-confirmed total remains in the approved on-page work statement, not metadata that could overstate what the 16 displayed edits alone account for. Case pages update their title, description, social-card title, image, and image alt text from the selected canonical work item.
- The mobile browser `theme-color` follows the actual dark-hero/light-page state; case pages remain `#F5F5F5`. The approved hero is preloaded and YouTube's image host is preconnected because thumbnails are the dominant remote media.
- Both pages expose a first-focus skip link to a real focusable heading (`#workTitle` / `#csTitle`). The homepage Lenis selector explicitly excludes `.skip-link`, preserving native fragment focus instead of producing a visual-only smooth scroll; `.skip-target` supplies fixed-nav clearance and a visible blue focus outline.
- Remote thumbnails always get two failure stages: try the canonical `thumbFallback`, then hide the broken image and expose a designed `.media-unavailable` surface. Keep rail titles visible on that surface; menu, current-project, source-poster, and next-project previews use restrained explicit unavailable labels. Never leave browser broken-image chrome visible.

### Continuous canvas (locked)

- The page's base field lives on `body::before` (fixed, z:-2): a NEUTRAL 160deg gradient only. Never put colored radials on a fixed layer — a viewport-glued glow "follows" the user while scrolling (reported bug). Accent light belongs to sections, scrolling with them. Sections NEVER own an opaque background (`.contact` had one — removed); they only add accents.
- Scroll-linked states must be kicked once after reveal (`kick()` rAF loop in initMotion) — otherwise a reload at a restored scroll position shows un-receded hero states until the first user scroll (reported bug).
- Everything hero-owned must dissolve BEFORE the hero's bottom edge: the ensemble mask ends transparent at 94%, `.hero-atmosphere` is masked out by 96%. Any hero layer that reaches 100% paints a visible band at the section boundary (pixel-verified seamless: Δ luminance ≈ 0.1/255 across the seam).
- Scroll exit: `initMotion`'s rAF also recedes `.hero-ensemble` (opacity → .5, translateY -24px, scale down as you scroll; inline `transition:none` is set on first scroll so the entrance transition never fights the scroll-link).
- Menu travel must NOT replay section entrances — it only forces `in-view` on the target's reveal elements. Re-running entrance animations on revisit reads as "the section reset" (user-reported bug, twice). First-time reveals happen only via the IntersectionObserver.
- The work stage disables pointer events until it is substantially visible (`intro > .55`) so it never blocks hero interactions across the -12svh overlap.

### Process

- Work one section per pass and screenshot-verify desktop and mobile before presenting.
- `works.json` remains the canonical source for featured work data.
- Keep changes deliberate and remove obsolete personal-portfolio behavior as sections are rebuilt.
